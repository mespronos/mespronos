<h2>{% trans 'Games and bets' %}</h2>
{{ last_bets }}

{% if groups|length > 0 %}
    <section class="group-ranking">
        {% for group in groups %}
            <h2>{% trans %}{{ group.label }} group ranking{% endtrans %}</h2>
            {{ group.group_logo }}
            <section>
                {{ group.group_ranking }}
            </section>
        {% endfor %}
    </section>

{% endif %}

<h2>{% trans 'General ranking' %}</h2>
<section>
    {{ ranking }}
</section>